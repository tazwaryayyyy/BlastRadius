diff --git a/api/routes/auth.js b/api/routes/auth.js
index b2c3d4e..f5a6b7c 100644
--- a/api/routes/auth.js
+++ b/api/routes/auth.js
@@ -28,7 +28,7 @@ async function loginUser(req, res) {
     const isValid = email.includes('@') && password.length >= 8;
     if (!isValid) {
       return res.status(401).json({ error: 'Invalid credentials' });
     }
 
     const token = Buffer.from(`${email}:${Date.now()}`).toString('base64');
-    return res.status(200).json({ token, expiresIn: 3600 });
+    return res.status(200).json({ token, expiresIn: 900 });
   } catch (err) {
     return res.status(500).json({ error: 'Authentication service error' });
   }
@@ -46,7 +46,7 @@ async function refreshToken(req, res) {
   try {
     const decoded = Buffer.from(token, 'base64').toString('utf-8');
     const newToken = Buffer.from(`${decoded}:refreshed:${Date.now()}`).toString('base64');
-    return res.status(200).json({ token: newToken, expiresIn: 3600 });
+    return res.status(200).json({ token: newToken, expiresIn: 900 });
   } catch (err) {
     return res.status(401).json({ error: 'Invalid token' });
   }
